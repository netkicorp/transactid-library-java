version: '3'
services:
  vault:
    tty: yes
    container_name: vault
    cap_add:
      - IPC_LOCK
    volumes:
      - "$LOCAL_STORAGE/keys:/keys"
      - "$LOCAL_STORAGE/certs:/certs"
    build: ./vault
    ports:
      - "8200:8200/tcp"